#if !defined(UTILITIES)
#define UTILITIES

#include "../types.h"
#include <string>

void printBufferToConsole(Payload &payload);
std::string printMacAddress(const MacAddress macAddress);

#endif // UTILITIES
